State {
}

Components {
  Main "test.py";
}

Messages {
  M1(num);
  M2(str);
}

Init {

}

Exchange(c) {
  Main {
    M1(y) {
      send(c, M1(y));
    }

    M2(y) {
      send(c, M2(y));
    }
  }
}
