State {
}

Components {
  Main "test.py";
}

Messages {
  M(str, str);
}

Init {

}

Exchange(c) {
  Main {
    M(a, b) {
      send(c, M("1", "a"));
      send(c, M("2", "b"));
      send(c, M("3", "c"));
    }
  }
}
