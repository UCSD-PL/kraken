Components {
  Main "test.py";
}

Messages {
  M(str, str);
}

Init {
  spawn(Main);
}

Exchange(c) {
  Main {
    M(a, b) {
      send(c, M("1", "a"));
      send(c, M("2", "b"));
      send(c, M("3", "c"));
    }
  }
}

Properties {
  Any =
    Match {
      .*
    };

  Any2 =
    Match {
     .*.*
    };

  AnyOrAny =
    Match {
     .*|.*
    };

  MaybeOneOrAny =
    Match {
      .?|.*
    };

  AnyDisguise =
    Match {
      ([^send(M(_, _))]|[^recv(M(_, _))])*
    };

  NoSendTillRecv =
    Match {
      [^send(M(_, _))]*|.*[recv(M(_, _))][^send(M(_, _))]*
    };

  DonTest =
    Match {
      (.*[recv(M(_, _))])?[^send(M(_, _))]*
    };
}
