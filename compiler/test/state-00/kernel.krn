Globals {
  cur_tab : num;
}

Messages {
  ReqCurTab();
  CurTab(num);
}

Init {
  cur_tab := 0;
}

Exchange(c) {
  ReqCurTab() {
    cur_tab := cur_tab + 1;
    send(c, CurTab(cur_tab));
  }

  # ignore
  CurTab(n) {
  }
}
