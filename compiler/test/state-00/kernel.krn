State {
  count : num;
}

Components {
  Main "test.py";
}

Messages {
  ReqCount();
  Count(num);
}

Init {
  count = 0;
  spawn(Main);
}

Exchange(c) {
  Main {
    ReqCount() {
      count = count + 1;
      send(c, Count(count));
    }
  }
}
