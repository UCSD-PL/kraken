State {
  curtab : num;
}

Components {
  Main "test.py";
}

Messages {
  ReqCurTab();
  CurTab(num);
}

Init {
  curtab := 0;
  spawn(Main);
}

Exchange(c) {
  Main {
    ReqCurTab() {
      curtab := curtab + 1;
      send(c, CurTab(curtab));
    }

    # ignore
    CurTab(n) {
    }
  }
}
