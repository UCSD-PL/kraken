WGET = "wget.py";

State {
}

Components {
  Main "test.py";
}

Messages {
  Wget(str);
  HTML(str, fdesc);
}

Init {
  spawn(Main);
}

Exchange(c) {
  Main {
    Wget(url) {
      f = call(WGET, url);
      send(c, HTML(url, f));
    }

    HTML(url, html) {
    }
  }
}
