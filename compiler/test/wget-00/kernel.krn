WGET = "wget.py";

State {
}

Components {
  Main "wget.py";
}

Messages {
  Wget(str);
  HTML(str, fdesc);
}

Init {

}

Exchange(c) {
  Main {
    Wget(url) {
      f = call(WGET, url);
      send(c, HTML(url, f));
    }

    HTML(url, html) {
    }
  }
}
